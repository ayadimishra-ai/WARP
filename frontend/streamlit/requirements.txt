# sk.lite — Python dependencies

# Core engine
pandas>=2.0.0
openpyxl>=3.1.0

# Streamlit frontend
streamlit>=1.35.0
plotly>=5.18.0

# Auth (bcrypt password hashing — replaces SHA-256)
# passlib is unmaintained and incompatible with bcrypt>=4.0 (72-byte limit error).
# Use bcrypt directly instead.
bcrypt>=4.0.0

# Flight API (optional — used for flight number lookup)
requests>=2.31.0

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0

# API endpoint (optional — only needed to run api.py REST server)
# pip install fastapi uvicorn
# fastapi>=0.110.0
# uvicorn>=0.27.0
